import React from 'react';

const Landing: React.FC = () => {
  // const sound = new Audio(content.audioContent);

  // const start = (): void => {
  //   sound.play();
  // };

  return (
    <>
      <h1>How to Pronounce</h1>
      <button onClick={() => console.log('click')}>Ouvir</button>
    </>
  );
};

export default Landing;
